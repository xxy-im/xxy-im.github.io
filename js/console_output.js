/* 控制台输出 */
if(window.console) {
	var cons = console; 
	if(cons) { 
		cons.group("Hello There !!!");
		cons.log("%cI'm xxy", "background-image: linear-gradient(#f39800, #f08300, #eb6238);font-size: 4rem;");
		cons.log("My WeChat: xxygobig");
		cons.groupEnd();
	}
}
